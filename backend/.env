const PORT = process.env.PORT ?? 4000;
const ALLOWED_ORIGIN = process.env.CORS_ORIGIN;

app.use(require('cors')({
    origin: ['http://localhost:3000', ALLOWED_ORIGIN].filter(Boolean),
    credentials: true,
}));

app.listen(PORT, ()=> console.log(`API on :${PORT}`));